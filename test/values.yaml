global:
  hosts:
    domain: "localhost"
    # gitlab:
    #   name: "gitlab.gitlab.ingress"
    # externalHostname: gitlab-webservice-default.gitlab:8181/
    externalIP: 0.0.0.0
    https: false
  ingress:
    configureCertmanager: false

certmanager:
  install: false

gitlab:
  webservice:
    ingress:
      enabled: true
      configureCertmanager: false
      hosts:
        - name: gitlab-webservice-default.gitlab:8181
          paths:
            - /
      tls: {}
gitlab-runner:
  runners:
    config: |
      [[runners]]
        name = "k3d-runner"
        url = "http://gitlab-webservice-default.gitlab:8181/"
        token = "glrt-RabUsNrTSUbKDvGQGhXvx286MQpwOjIKdDozCnU6MQ8.01.171rdmkij"
        executor = "docker"
        [runners.custom_build_dir]
        [runners.docker]
          tls_verify = false
          image = "alpine:latest"
          privileged = true
          volumes = ["/cache"]
